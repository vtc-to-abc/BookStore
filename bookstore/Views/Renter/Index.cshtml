@model IEnumerable<bookstore.Models.Renter>;

@*   *@

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="row">
    <div class="col-md-8 offset-md-2">
        <table class="table">
            <thead>
                <tr class="text-center">
                    <th>@Html.DisplayNameFor(model => model.renter_id)</th>
                    <th>@Html.DisplayNameFor(model => model.renter_name)</th>
                    <th>@Html.DisplayNameFor(model => model.renter_phone)</th>
                    <th>@Html.DisplayNameFor(model => model.renter_email)</th>
                </tr>
            </thead>
                @foreach(var item in Model)
                {
                    <tr>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.renter_id)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem=> item.renter_name)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem=> item.renter_phone)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem=> item.renter_email)
                        </td>
                        <td class="align-middle">
                            <a class="btn btn-outline-primary" asp-controller="Renter" asp-action="MakeOrder" asp-route-id="@item.renter_id"><i class ="bi bi-pencil-square"></i>Order</a>
                            <a class="btn btn-outline-primary" asp-controller="Renter" asp-action="Edit" asp-route-id="@item.renter_id"><i class ="bi bi-pencil-square"></i>Edit</a>
                            <a class="btn btn-outline-info" asp-controller="Renter" asp-action="Details" asp-route-id="@item.renter_id"><i class ="bi bi-pencil-square"></i> Details</a>
                            <a class= "btn btn-danger text-white" asp-controller="Renter" asp-action="Delete" asp-route-id="@item.renter_id"><i class ="bi bi-pencil-square"></i>Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<div class="text-white">
    <a asp-controller="Renter" asp-action="Create" class="btn btn-success"><i class="bi bi-plus-circle"></i> Add New</a>

</div>